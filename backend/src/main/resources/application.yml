server:
  port: 8080
spring:
  profiles:
    active: db
  application:
    name: subway-ticket-backend
  autoconfigure:
    exclude: []
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
